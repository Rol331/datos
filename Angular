Comandos angular 

ng new proyecto001.  --> Crear Proyecto
ng serve -o ---> arrancar un servidor

ng generate component nombrecomponente ---> crear un componente 
ng generate module elementos ---> crear un modulo 

ng generate component elementos/dado ---> crear un componente en un determinado modulo 

 ng generate service lista ----> crear una Lista
 
 ng generate pipe letras. ---> crear pipes

Dentro de las dos llaves abiertas y cerradas Angular nos permite efectuar una operación:

<p>En los últimos 3 meses ha ganado: {{sueldos[0]+sueldos[1]+sueldos[2]}}</p> 

Tambine se puede colocar en las propiedades 

<p>Puede visitar el sitio ingresando <a href="{{sitio}}">aquí</a></p>

