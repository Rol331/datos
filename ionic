 ionic start myApp blank ---> crear un nuevo proyecto
 
 ionic start myApp tabs
 
 ionic start myApp sidemenu ---> por defecto ionic puede crear dos tipos de proyecto es como una ayuda
 
  ionic platform add <nombre-de-la-plataforma>. ---> compilar nuestro proyecto para una plataforam especial
  
  ionic platform. ---> ver el listado de las plataforma instaladas 
  
  ionic serve --> ejecutar un servidor en local
  
  
  ionic build <PLATFORM> --> generar un ejcutble directamente 
    
   inic g c --> crea la carpeta y el componenete
   
   ionic g m <backend>   --> crea un modulo en la carpeta indicada
    
   ionic g s /nombrecarpeta/nombre  ---> cre un servicio 
   
   ionic build --producto ---> generar todos los archvios de producción 
   npx cap add android    ---> generar proyecto para android stuido 
   npx cap add ios        ----> gnerar proyecto para xcode 

   npx cap open ios --> abrir poyecto en xcode
   npx cap open android --> abrir proyecto en androidstudio
   
   npm install -g @ionic/cli native-run  --->instalar libreria para cambios en linea
   
   ionic cap run android -l --external 
   
   ionic cap run ios -l --external
   
   
